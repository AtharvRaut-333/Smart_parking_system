pipeline {
    agent any

    tools {
        // Use the exact tool names configured in Jenkins Global Tool Configuration
        maven 'maven3'
        jdk 'JDK17'
    }

    environment {
        NEXUS_DOCKER_REGISTRY = 'nexus.imcc.com'
        DOCKER_REPO = 'smart_parking_system_2401169'
        DOCKER_IMAGE_NAME = "${DOCKER_REPO}/smart-parking-backend"
        NEXUS_CREDENTIALS_ID = 'nexus-credentials'
        SONAR_HOST_URL = 'http://localhost:9000'
        SONAR_CREDENTIALS_ID = 'sonarqube-token'
        GITHUB_CREDENTIALS_ID = 'github-token'
        NEXUS_URL = 'http://nexus.imcc.com'
        NEXUS_REPOSITORY = 'maven-snapshots'
        APP_NAME = 'smart-parking-backend'
        BUILD_VERSION = "${env.BUILD_NUMBER}"
    }

    triggers {
        pollSCM('H/5 * * * *')
    }

    stages {
        stage('Checkout') {
            steps {
                echo '📥 Checking out code from GitHub...'
                checkout scm
                script {
                    env.GIT_COMMIT = sh(script: 'git rev-parse --short HEAD', returnStdout: true).trim()
                    env.GIT_COMMIT_MSG = sh(script: 'git log -1 --pretty=%B', returnStdout: true).trim()
                    env.GIT_AUTHOR = sh(script: 'git log -1 --pretty=%an', returnStdout: true).trim()
                }
            }
        }

        stage('Build') {
            steps {
                echo '🔨 Building application with Maven...'
                dir('smark-parking-backend') {
                    sh 'mvn -B -DskipTests clean compile'
                }
            }
        }

        stage('Unit Tests') {
            steps {
                echo '🧪 Running unit tests...'
                dir('smark-parking-backend') {
                    sh 'mvn -B test'
                }
            }
            post {
                always {
                    dir('smark-parking-backend') {
                        junit '**/target/surefire-reports/*.xml'
                    }
                }
            }
        }

        stage('SonarQube Analysis') {
            steps {
                echo '🔍 Running SonarQube code analysis...'
                dir('smark-parking-backend') {
                    withCredentials([string(credentialsId: SONAR_CREDENTIALS_ID, variable: 'SONAR_TOKEN')]) {
                        sh """
                            mvn -B sonar:sonar \
                                -Dsonar.projectKey=smart-parking-backend \
                                -Dsonar.projectName="Smart Parking Backend" \
                                -Dsonar.host.url=${SONAR_HOST_URL} \
                                -Dsonar.login=${SONAR_TOKEN}
                        """
                    }
                }
            }
        }

        stage('Quality Gate') {
            steps {
                echo '🚦 Checking SonarQube Quality Gate...'
                timeout(time: 5, unit: 'MINUTES') {
                    waitForQualityGate abortPipeline: true
                }
            }
        }

        stage('Package') {
            steps {
                echo '📦 Packaging application...'
                dir('smark-parking-backend') {
                    sh 'mvn -B -DskipTests package'
                }
            }
        }

        stage('Deploy to Nexus (Maven)') {
            steps {
                echo '📤 Deploying artifact to Nexus...'
                dir('smark-parking-backend') {
                    sh """
                        mvn -B deploy -DskipTests \
                            -DaltDeploymentRepository=nexus::default::${NEXUS_URL}/repository/${NEXUS_REPOSITORY}
                    """
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo '🐳 Building Docker image...'
                dir('smark-parking-backend') {
                    script {
                        dockerImage = docker.build("${env.NEXUS_DOCKER_REGISTRY}/${env.DOCKER_IMAGE_NAME}:${env.GIT_COMMIT}")
                        sh "docker tag ${env.NEXUS_DOCKER_REGISTRY}/${env.DOCKER_IMAGE_NAME}:${env.GIT_COMMIT} ${env.NEXUS_DOCKER_REGISTRY}/${env.DOCKER_IMAGE_NAME}:latest"
                    }
                }
            }
        }

        stage('Push to Nexus Docker Registry') {
            steps {
                echo '🚀 Pushing Docker image to Nexus...'
                script {
                    docker.withRegistry("http://${env.NEXUS_DOCKER_REGISTRY}", env.NEXUS_CREDENTIALS_ID) {
                        dockerImage.push("${env.GIT_COMMIT}")
                        dockerImage.push("latest")
                    }
                }
            }
        }

        stage('Deploy to EC2') {
            steps {
                echo '🌐 Deploying to EC2 (local Docker host)...'
                script {
                    sh """
                        docker stop ${env.APP_NAME} || true
                        docker rm ${env.APP_NAME} || true
                        docker run -d \
                            --name ${env.APP_NAME} \
                            -p 8081:8081 \
                            --restart unless-stopped \
                            -e SPRING_PROFILES_ACTIVE=prod \
                            ${env.NEXUS_DOCKER_REGISTRY}/${env.DOCKER_IMAGE_NAME}:latest
                    """
                }
            }
        }

        stage('Health Check') {
            steps {
                echo '❤️ Performing health check...'
                script {
                    sleep(time: 15, unit: 'SECONDS')
                    sh 'curl -f http://localhost:8081/actuator/health || exit 1'
                }
            }
        }
    }

    post {
        success { echo '✅ Pipeline completed successfully!' }
        failure {
            echo '❌ Pipeline failed!'
            script {
                sh """
                    docker stop ${env.APP_NAME} || true
                    docker rm ${env.APP_NAME} || true
                """
            }
        }
        always {
            echo '🧹 Cleaning up workspace...'
            cleanWs()
        }
    }
}
