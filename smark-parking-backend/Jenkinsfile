pipeline {
    agent any
    
    tools {
        maven 'Maven3'
        jdk 'JDK17'
    }
    
    environment {
        // Nexus settings
        NEXUS_DOCKER_REGISTRY = 'nexus.imcc.com'                         // host only (no path)
        DOCKER_REPO = 'smart_parking_system_2401169'                     // path-based repo name
        DOCKER_IMAGE_NAME = "${DOCKER_REPO}/smart-parking-backend"       // full image path used for tagging
        // Jenkins credential ID for Nexus (username/password)
        NEXUS_CREDENTIALS_ID = 'nexus-credentials'
        
        // SonarQube (Jenkins credential ID for token)
        SONAR_HOST_URL = 'http://localhost:9000'
        SONAR_CREDENTIALS_ID = 'sonarqube-token'
        
        // GitHub token credential id (used by multibranch / plugins; not used directly in this Jenkinsfile)
        GITHUB_CREDENTIALS_ID = 'github-token'
        
        // Nexus Maven Repository (for mvn deploy)
        NEXUS_URL = 'http://nexus.imcc.com'
        NEXUS_REPOSITORY = 'maven-snapshots'
        
        // Application
        APP_NAME = 'smart-parking-backend'
        BUILD_VERSION = "${env.BUILD_NUMBER}"
    }
    
    triggers {
        // Poll SCM every 5 minutes (can be replaced with GitHub webhook)
        pollSCM('H/5 * * * *')
    }
    
    stages {
        stage('Checkout') {
            steps {
                echo '📥 Checking out code from GitHub...'
                checkout scm
                script {
                    env.GIT_COMMIT = sh(script: 'git rev-parse --short HEAD', returnStdout: true).trim()
                    env.GIT_COMMIT_MSG = sh(script: 'git log -1 --pretty=%B', returnStdout: true).trim()
                    env.GIT_AUTHOR = sh(script: 'git log -1 --pretty=%an', returnStdout: true).trim()
                }
            }
        }
        
        stage('Build') {
            steps {
                echo '🔨 Building application with Maven...'
                dir('smark-parking-backend') {
                    sh 'mvn -B -DskipTests clean compile'
                }
            }
        }
        
        stage('Unit Tests') {
            steps {
                echo '🧪 Running unit tests...'
                dir('smark-parking-backend') {
                    sh 'mvn -B test'
                }
            }
            post {
                always {
                    dir('smark-parking-backend') {
                        junit '**/target/surefire-reports/*.xml'
                    }
                }
            }
        }
        
        stage('SonarQube Analysis') {
            steps {
                echo '🔍 Running SonarQube code analysis...'
                dir('smark-parking-backend') {
                    // Use Jenkins credential ID SONAR_CREDENTIALS_ID to inject the token
                    withCredentials([string(credentialsId: SONAR_CREDENTIALS_ID, variable: 'SONAR_TOKEN')]) {
                        sh """
                            mvn -B sonar:sonar \
                                -Dsonar.projectKey=smart-parking-backend \
                                -Dsonar.projectName="Smart Parking Backend" \
                                -Dsonar.host.url=${SONAR_HOST_URL} \
                                -Dsonar.login=${SONAR_TOKEN}
                        """
                    }
                }
            }
        }
        
        stage('Quality Gate') {
            steps {
                echo '🚦 Checking SonarQube Quality Gate...'
                timeout(time: 5, unit: 'MINUTES') {
                    waitForQualityGate abortPipeline: true
                }
            }
        }
        
        stage('Package') {
            steps {
                echo '📦 Packaging application...'
                dir('smark-parking-backend') {
                    sh 'mvn -B -DskipTests package'
                }
            }
        }
        
        stage('Deploy to Nexus (Maven)') {
            steps {
                echo '📤 Deploying artifact to Nexus...'
                dir('smark-parking-backend') {
                    sh """
                        mvn -B deploy -DskipTests \
                            -DaltDeploymentRepository=nexus::default::${NEXUS_URL}/repository/${NEXUS_REPOSITORY}
                    """
                }
            }
        }
        
        stage('Build Docker Image') {
            steps {
                echo '🐳 Building Docker image...'
                dir('smark-parking-backend') {
                    script {
                        // build image and tag by commit SHA, then tag latest
                        dockerImage = docker.build("${NEXUS_DOCKER_REGISTRY}/${DOCKER_IMAGE_NAME}:${GIT_COMMIT}")
                        sh "docker tag ${NEXUS_DOCKER_REGISTRY}/${DOCKER_IMAGE_NAME}:${GIT_COMMIT} ${NEXUS_DOCKER_REGISTRY}/${DOCKER_IMAGE_NAME}:latest"
                    }
                }
            }
        }
        
        stage('Push to Nexus Docker Registry') {
            steps {
                echo '🚀 Pushing Docker image to Nexus...'
                script {
                    // docker.withRegistry will use the Jenkins credential id to log in
                    docker.withRegistry("http://${NEXUS_DOCKER_REGISTRY}", NEXUS_CREDENTIALS_ID) {
                        dockerImage.push("${GIT_COMMIT}")
                        dockerImage.push("latest")
                    }
                }
            }
        }
        
        stage('Deploy to EC2') {
            steps {
                echo '🌐 Deploying to EC2 (local Docker host)...'
                script {
                    // stop, remove if exists, then run the new image
                    sh """
                        docker stop ${APP_NAME} || true
                        docker rm ${APP_NAME} || true
                        docker run -d \
                            --name ${APP_NAME} \
                            -p 8081:8081 \
                            --restart unless-stopped \
                            -e SPRING_PROFILES_ACTIVE=prod \
                            ${NEXUS_DOCKER_REGISTRY}/${DOCKER_IMAGE_NAME}:latest
                    """
                }
            }
        }
        
        stage('Health Check') {
            steps {
                echo '❤️ Performing health check...'
                script {
                    sleep(time: 15, unit: 'SECONDS')
                    sh 'curl -f http://localhost:8081/actuator/health || exit 1'
                }
            }
        }
    }
    
    post {
        success {
            echo '✅ Pipeline completed successfully!'
        }
        failure {
            echo '❌ Pipeline failed!'
            script {
                // rollback: stop and remove failed container if created
                sh """
                    docker stop ${APP_NAME} || true
                    docker rm ${APP_NAME} || true
                """
            }
        }
        always {
            echo '🧹 Cleaning up workspace...'
            cleanWs()
        }
    }
}
